[defaults]
inventory = inventories/gcp.yml
roles_path = roles
host_key_checking = False
private_key_file = ~/.ssh/google_compute_engine
retry_files_enabled = False
stdout_callback = yaml
timeout = 30

[privilege_escalation]
become = True
become_user = root
